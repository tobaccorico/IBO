
# Maps secret names used in workflow code to environment variables.
# Actual values go in .env (never committed).

secretsNames:
  CMC_API_KEY:
    - CMC_API_KEY_VAR
  CG_API_KEY:
    - CG_API_KEY_VAR
