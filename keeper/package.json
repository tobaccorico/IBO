{
  "name": "quid-keeper",
  "version": "1.0.0",
  "description": "QU!D Protocol Keeper Bot - monitors positions and clears swaps",
  "main": "keeper.js",
  "scripts": {
    "start": "npx ts-node keeper.ts",
    "build": "tsc keeper.ts",
    "dev": "npx ts-node --watch keeper.ts"
  },
  "dependencies": {
    "ethers": "^6.9.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
