
[profile.default]
src = "src"
out = "out"

libs = ["lib"]
ffi = true

gas_reports = ["*"]
fs_permissions = [
    { access = "read-write", path = ".forge-snapshots/"},
    { access = "read-write", path = "demo/src/contracts/"},
    { access = "read-write", path = "src/contracts/"},
    { access = "read-write", path = "deployments/"},
    { access = "read-write", path = "./"}
]

solc_version = "0.8.26"
evm_version = "cancun"

via_ir = false
optimizer = true
optimizer_runs = 200
dynamic_test_linking = true

bytecode_hash = "none"
deny_warnings = false
cbor_metadata = false
sparse_mode = false
build_info = true

[rpc_endpoints]
# forge script scripts/DeployL1.s.sol:Deploy --rpc-url mainnet --broadcast --verify
mainnet = "https://ethereum-rpc.publicnode.com"
arbitrum = "https://arbitrum-one-rpc.publicnode.com"
base = "https://base-rpc.publicnode.com"
celo = "https://celo-rpc.publicnode.com"
polygon = "https://polygon-bor-rpc.publicnode.com"
unichain = "https://unichain-rpc.publicnode.com"

[etherscan]
mainnet = { key = "${ETHERSCAN_L1}" }
arbitrum = { key = "${ETHERSCAN_L1}" }
base = { key = "${ETHERSCAN_L1}" }
celo = { key = "${ETHERSCAN_L1}" }
polygon = { key = "${ETHERSCAN_L1}" }
unichain = { key = "${ETHERSCAN_L1}" }

[profile.dependencies]
v4-core = { git = "https://github.com/uniswap/v4-core" }
v4-periphery = { git = "https://github.com/uniswap/v4-periphery" }
aave-v3-origin = { git = "https://github.com/aave-dao/aave-v3-origin" }
solidity-math-utils = { git = "https://github.com/barakman/solidity-math-utils/" }
